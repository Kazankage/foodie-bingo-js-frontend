fetch('http://localhost:3000/bingos')
.then(res => res.json())
.then(bingos => {
    bingos.forEach(bingo => 
        
        const bingo = { id, name, city, country, specialty, price, rating, hype }

    )
})
